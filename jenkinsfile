node{
    stage('build now') {
https://github.com/pavanireddykonda35/raki.git
    }
    stage('build mvn') {
sh 'mvn package'
    }
    stag('test result') {
    junit 'target/*surefire-reports/*.xml'
    }
    stage( 'archifacts') {
        archiveArtifacts 'target/*.jar'
    }
}
